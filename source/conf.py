html_theme = "basic"  # Use the "basic" theme as a base
html_theme_path = []
html_static_path = ["_static"]  # For custom CSS, images, JS
templates_path = ["_templates"]
html_sidebars = {}  # Disable sidebar
html_context = {
    "readthedocs_show_version_warning": False,  # Disable version warning
    "readthedocs_add_google_analytics": False,  # Disable analytics (optional)
    "readthedocs_disable_flyout": True,         # Disable the flyout menu
}
